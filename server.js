const express = require("express"); const app = express(); const PORT = process.env.PORT || 8000; app.get("/", (req, res) => { res.json({ message: "LGKA Backend Working!", port: PORT, railway: "connected" }); }); app.get("/api/health", (req, res) => { res.json({ status: "healthy", port: PORT }); }); app.listen(PORT, "0.0.0.0", () => { console.log("LGKA Backend running on port " + PORT); });
